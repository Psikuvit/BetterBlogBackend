version: '3.8'

services:
  # MongoDB service
  mongodb:
    image: mongo:7.0
    container_name: betterblog-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
      MONGO_INITDB_DATABASE: betterblog
    volumes:
      - mongodb_data:/data/db
    networks:
      - betterblog-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  # Spring Boot application
  betterblog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: betterblog-app
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_MONGODB_URI: mongodb://admin:password123@mongodb:27017/betterblog?authSource=admin
      SPRING_MONGODB_DATABASE: betterblog
      CORS_ALLOWED_ORIGINS: http://localhost:3000,http://localhost:8080
      JWT_SECRET: hy1F--_ZKej:8+4U3$JzpGyU-2ig9q@]gqq9/f$lD;n
      JWT_EXPIRATION: 86400000
      JWT_REFRESH_EXPIRATION: 604800000
      SPRING_MAIL_HOST: mailhog
      SPRING_MAIL_PORT: 1025
      SPRING_MAIL_SMTP_AUTH: false
      SPRING_MAIL_SMTP_STARTTLS_ENABLE: false
      MAIL_FROM: no-reply@betterblog.local
      PASSWORD_RESET_CODE_TTL_MINUTES: 30
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - betterblog-network
    restart: unless-stopped

  # Local SMTP server for testing password reset emails
  mailhog:
    image: mailhog/mailhog:latest
    container_name: betterblog-mailhog
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - betterblog-network
    restart: unless-stopped

volumes:
  mongodb_data:
    driver: local

networks:
  betterblog-network:
    driver: bridge

